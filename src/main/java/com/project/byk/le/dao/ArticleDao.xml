<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.byk.le.dao.ArticleDao">
	<select id="getArticlesByCode" resultType="Article">
		SELECT *
		FROM article
		WHERE boardCode = #{code}
		AND displayStatus = 1
		AND delStatus = 0
		ORDER BY id DESC
	</select>

	<insert id="write" useGeneratedKeys="true" keyProperty="id">
		INSERT
		INTO article
		SET regDate = NOW(),
		updateDate = NOW(),
		displayStatus = 1,
		title = #{title},
		body = #{body},
		boardCode = #{code},
		<!-- 로그인후 글쓰기가능 설정수 수정필요 -->
		memberId = 1
	</insert>

	<select id="getArticleById" resultType="Article">
		SELECT *
		FROM article
		WHERE id = #{id}
	</select>
</mapper>